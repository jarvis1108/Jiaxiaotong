package com.dxtwangxiao.strongestsuperscholar.module.base.dao;

import com.dxtwangxiao.strongestsuperscholar.module.base.entity.BookVerInfo;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.List;

import static org.junit.Assert.*;

/**
 * @Author: meiyu
 * @Date: 2018/7/13 16:09
 */
@SpringBootTest
@RunWith(SpringRunner.class)
public class BookVerInfoRepositoryTest {

    @Autowired
    private BookVerInfoRepository bookVerInfoRepository;

    @Test
    public void findAll() {
        List<BookVerInfo> bookVerInfoList = bookVerInfoRepository.findAll();
        assertNotEquals(1,bookVerInfoList.size());
        bookVerInfoList.forEach(System.out::println);

    }

    @Test
    public void save() {
        BookVerInfo bookVerInfo = new BookVerInfo();
        bookVerInfo.setBookVerId("1");
        bookVerInfo.setBookVerName("人教版");
        bookVerInfo.setCreateUser("test");
        BookVerInfo result = bookVerInfoRepository.save(bookVerInfo);
        System.out.println(result);
    }

    @Test
    public void findByBookVerId() {
        String bookVerId = "1";
        BookVerInfo bookVerInfo= bookVerInfoRepository.findByBookVerId(bookVerId);
        assertNotNull(bookVerInfo);
        System.out.println(bookVerInfo);
    }
}