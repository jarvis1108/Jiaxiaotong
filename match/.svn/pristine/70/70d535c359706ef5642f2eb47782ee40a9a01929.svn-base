/** 
 * @file
 * @description <br/> //  ******************************************************************
 *              <br/> //  * 功能说明：最强学霸接口设计_私有题库模块
 *              <br/> //  * 创建标志：孙伟浩 2018年10月13日创建
 *              <br/> //  * 标志编号：
 *              <br/> //  * 标志描述：
 *              <br/> //  * 附注说明：选择题部分用例接口设计
 *              <br/> //  *          （1）本文件的接口清单如下：
 *              <br/> //  *               (a) ListRuleSetting : 获取规则
 *              <br/> //  *               (b) UpdateRuleSetting : 更新规则
 *              <br/> //  *              
 *              <br/> //  * 当前状态：【编码中】【编码完成】【测试完】【可以试用】【正式使用√】【作废停用】
 *              <br/> //  ******************************************************************
 * @author 孙伟浩
 * @copyright 暂无
 */

/**
 * @class
 * @summary 实现功能：后台管理系统规则设置模块接口设计
 * @memberOf BAM                      
 */
function BAM_RUL_010() {

    /**
     * @summary 实现功能：获取规则列表
     * @description **GET** /api/v1/bam/ruleSetting/list
     * @param {object} objParam                                        要传入的参数
     * @param {string} objParam.reqId                                  要传入的参数.请求的Id
     * @param {string} objParam.reqType                                要传入的参数.请求类型 固定值为: BAM_RUL_010_ListRuleSetting
     * @param {object} objParam.reqUserInfo                            要传入的参数.当前用户信息
     * @param {string} objParam.reqUserInfo.userId                     要传入的参数.当前用户信息.用户Id
     * @param {object} objParam.reqParam                               要传入的参数.请求的业务参数(无)
     * @param {object} objParam.reqRefData                             要传入的参数.存放业务以外的其它信息 default: {}
     * @param {object} objParam.reqPageInfo                            要传入的参数.分页信息(无)

     *
     * @param {object} objResult                                                     返回结果
     * @param {string} objResult.resID                                               返回结果.响应ID
     * @param {string} objResult.reqId                                               返回结果.请求的ID
     * @param {string} objResult.reqType                                             返回结果.请求类型
     * @param {Object} objResult.resResult                                           返回结果.响应返回的业务数据
     * @param {object} objResult.resResult.curData                                   返回结果.响应返回的业务数据.当前业务结果
     * @param {string} objResult.resResult.curData.questionNumber                    返回结果.响应返回的业务数据.当前业务结果.战局题目数目
     * @param {string} objResult.resResult.curData.maxMatchCount                     返回结果.响应返回的业务数据.当前业务结果.最大匹配次数
     * @param {string} objResult.resResult.curData.maxMatchTime                      返回结果.响应返回的业务数据.当前业务结果.最长匹配时间   
     * @param {object} objResult.resResult.errorInfo                                 返回结果.响应返回的业务数据.当前业务结果.出错信息(具体结构框架统一定义)
     * @param {object} objResult.resRefData                                          返回结果.存放业务以外的其它信息(预留扩展，一般情况下不用) default: {}
     * @version 1.0
     * @example 暂无
     */
    function ListRuleSetting(objParam) {
        var objResult = {};
        return objResult;
    }


    /**
     * @summary 实现功能：更新规则
     * @description **POST** /api/v1/bam/ruleSetting
     * @param {object} objParam                                        要传入的参数
     * @param {string} objParam.reqId                                  要传入的参数.请求的Id
     * @param {string} objParam.reqType                                要传入的参数.请求类型 固定值为: BAM_RUL_010_UpdateRuleSetting
     * @param {object} objParam.reqUserInfo                            要传入的参数.当前用户信息
     * @param {string} objParam.reqUserInfo.userId                     要传入的参数.当前用户信息.用户Id
     * @param {object} objParam.reqParam                               要传入的参数.请求的业务参数
     * @param {object} objParam.reqParam.questionNumber                要传入的参数.请求的业务参数.战局题目数目
     * @param {object} objParam.reqParam.maxMatchCount                 要传入的参数.请求的业务参数.最大匹配次数
     * @param {object} objParam.reqParam.maxMatchTime                  要传入的参数.请求的业务参数.最长匹配时间
     * @param {object} objParam.reqRefData                             要传入的参数.存放业务以外的其它信息 default: {}
     * @param {object} objParam.reqPageInfo                               要传入的参数.分页信息(无)

     *
     * @param {object} objResult                                                     返回结果
     * @param {string} objResult.resID                                               返回结果.响应ID
     * @param {string} objResult.reqId                                               返回结果.请求的ID
     * @param {string} objResult.reqType                                             返回结果.请求类型
     * @param {Object} objResult.resResult                                           返回结果.响应返回的业务数据
     * @param {object} objResult.resResult.curData                                   返回结果.响应返回的业务数据.当前业务结果
     * @param {string} objResult.resResult.curData.questionNumber                    返回结果.响应返回的业务数据.当前业务结果.战局题目数目
     * @param {string} objResult.resResult.curData.maxMatchCount                     返回结果.响应返回的业务数据.当前业务结果.最大匹配次数
     * @param {string} objResult.resResult.curData.maxMatchTime                      返回结果.响应返回的业务数据.当前业务结果.最长匹配时间   
     * @param {object} objResult.resResult.errorInfo                                 返回结果.响应返回的业务数据.当前业务结果.出错信息(具体结构框架统一定义)
     * @param {object} objResult.resRefData                                          返回结果.存放业务以外的其它信息(预留扩展，一般情况下不用) default: {}
     * @version 1.0
     * @example 暂无
     */
    function UpdateRuleSetting(objParam) {
        var objResult = {};
        return objResult;
    }
}