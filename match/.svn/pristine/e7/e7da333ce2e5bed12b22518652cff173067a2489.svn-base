package com.dxtwangxiao.strongestsuperscholar.module.system.service;

import com.dxtwangxiao.strongestsuperscholar.module.system.entity.UserLoginRecord;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.List;

import static org.junit.Assert.*;

/**
 * 用户登陆记录服务接口
 * <p>
 * Created by Devin
 * 2018-07-03 下午 04:57
 */
@RunWith(SpringRunner.class)
@SpringBootTest
public class UserLoginRecordServiceTest {

    @Autowired
    private UserLoginRecordService userLoginRecordService;

    @Test
    public void getUserLoginRecordList() {
        List<UserLoginRecord> userLoginRecordList = userLoginRecordService.getAllUserLoginRecords();
        assertNotEquals(0, userLoginRecordList.size());

        userLoginRecordList.forEach(System.out::println);
    }

    @Test
    public void getAllUserLoginRecords() {
        List<UserLoginRecord> userLoginRecordList = userLoginRecordService.getAllUserLoginRecords();
        assertNotEquals(0, userLoginRecordList.size());

        userLoginRecordList.forEach(System.out::println);
    }

    @Test
    public void create() {
    }

    @Test
    public void update() {
    }
}